{
  "name": "lightning-out-host",
  "version": "0.1.0",
  "description": "Minimal host + server to run Lightning Out 2.0 LWCs in an external app (frontdoor exchange + demo page).",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch server.js --watch src --ext js,html --signal SIGINT server.js",
    "lint": "echo \"no linter configured\" && exit 0"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "dotenv": "^16.1.4",
    "express": "^4.18.2",
    "express-session": "^1.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "keywords": [
    "salesforce",
    "lightning-out",
    "lightning-out-2.0",
    "lwc",
    "frontdoor"
  ],
  "author": "",
  "license": "MIT"
}
